# Spec Kit benchmarking configuration
# Copy to .env and adjust as needed.

# Override the TerminalBench dataset (default: terminal-bench-core)
# SPEC_BENCH_DATASET=terminal-bench-core

# Comma-separated list of scenarios to execute by default
# SPEC_BENCH_SCENARIOS=specify,plan,tasks

# Agent identifier to load (defaults to spec-kit-orchestrator)
# SPEC_BENCH_AGENT=spec-kit-orchestrator

# Downstream LLM model alias understood by LiteLLM
# SPEC_BENCH_MODEL=anthropic/claude-3-5-sonnet

# Storage locations for artifacts and baseline records
# SPEC_BENCH_ARTIFACTS=.terminalbench
# SPEC_BENCH_BASELINES=specs/benchmarks

# Incident notification target (Slack channel, webhook, etc.)
# SPEC_BENCH_INCIDENT_CHANNEL=#spec-kit-benchmarks
